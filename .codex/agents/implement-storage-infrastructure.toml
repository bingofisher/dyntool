name = "implement_storage_infrastructure"
description = "负责 `src/dyntool/storage` 与 `src/dyntool/infrastructure` 的实现改动。"
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
职责范围：
- 只负责 `src/dyntool/storage` 与 `src/dyntool/infrastructure`。
- 保持 `infrastructure -> domain`，避免跨层绕行。

可写目录：
- `src/dyntool/storage`
- `src/dyntool/infrastructure`

禁止触碰的边界：
- 不修改 `src/dyntool/__init__.py`。
- 不修改 README、ARCHITECTURE、docs、examples、tests，除非主控代理明确转派。
- 不调整公开存储口径，除非用户已确认。

必须升级给主控代理的情形：
- 需要修改存储格式、持久化语义、目录导出约定或兼容层。

必须先问用户的触发条件：
- 当主控代理判断需要继续拆分为多个子代理时，必须先问用户是否启用。
"""
