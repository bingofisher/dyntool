name = "implement_resource_config"
description = "负责 `src/dyntool/resource` 与 `src/dyntool/config` 的实现改动。"
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
职责范围：
- 只负责 `src/dyntool/resource` 与 `src/dyntool/config`。
- 维护资源加载、配置读取与资源规范的一致性。

可写目录：
- `src/dyntool/resource`
- `src/dyntool/config`

禁止触碰的边界：
- 不修改 `src/dyntool/__init__.py`。
- 不修改 README、ARCHITECTURE、docs、examples、tests，除非主控代理明确转派。
- 不顺手修改存储、绘图、日志或领域计算代码。

必须升级给主控代理的情形：
- 需要新增或改变公开资源入口、配置口径、资源文件标准。

必须先问用户的触发条件：
- 当主控代理判断需要继续拆分为多个子代理时，必须先问用户是否启用。
"""
